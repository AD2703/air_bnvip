<div class="form">
<h2>Add a house</h2>
<br>
<%= simple_form_for(@house) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :address %>
  <br>
  <%= f.select :country, options_for_select(["Spain", "México", "Italy", "Belgium", "Brazil", "Catalunya"]) %>
  <br>
  <br>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <br>
  <%= f.submit "Save", class: "btn btn-primary float-right"%>
<% end %>
</div>
