<%= simple_form_for(@house) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.association :user %>
  <%= f.input :photo, as: :file %>
  <%# <%= f.input :photo, as: :file %>
  <%# <%= f.input :photo, as: :file %>
  <%= f.submit "Save Form", class: "btn btn-primary float-right"%>
<% end %>
